import { CustomThemeJson } from 'components/theme/theme.interface';

export const newTheme: CustomThemeJson = {
  name: 'lol',
  styles: {
    head: {
      color: 'red',
      fontSize: '18px',
      background: '#ff000066',
    },
  },
};
